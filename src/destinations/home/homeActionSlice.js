import { createSlice } from "@reduxjs/toolkit";

const initialState = {};

const homeActionSlice = createSlice({
  name: "home",
  initialState,
  reducers: {},
});

export default homeActionSlice.reducer;
